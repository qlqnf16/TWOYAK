import React from "react";

const PastDrugList = ({ drugs }) => {
  console.log(drugs[0].to);

  // drugs.forEach(drug => {
  //     condrug.from
  // })

  return <div>pastdruglist</div>;
};

export default PastDrugList;
